import Foundation



let n = Int(readLine()!)!
var ArrayName = readLine()!.components(separatedBy: " ").map{ Int($0)! }


ArrayName = ArrayName.reversed()




for element in ArrayName {
    print(element, terminator: " ")
}
